package tests;

	import org.openqa.selenium.edge.EdgeDriver;
	import org.openqa.selenium.WebDriver;
	import org.openqa.selenium.remote.RemoteWebDriver;

	public class OpenEdgeBrowser2
	{
		public static void main(String[] args)
		{
			//Open Edge browser
			EdgeDriver driver=new EdgeDriver();
			//SearchContext driver=new EdgeDriver();//Create an object to interface using concrete class's constructor
			//WebDriver driver=new EdgeDriver(); //Create an object to interface using concrete class's constructor
			//ChromiumDriver driver=new EdgeDriver();//Create an object to class using child class's constructor
			//RemoteWebDriver driver=new EdgeDriver();//Create an object to class using child class's constructor
		}
	}


